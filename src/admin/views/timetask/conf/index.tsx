import React from "react"
import { connect } from "react-redux";


class TimeTaskConfPage extends React.Component {
  render() {
    return (
      <div>
        TimeTaskConfPage
      </div>
    );
  }
}

export default connect()(TimeTaskConfPage)