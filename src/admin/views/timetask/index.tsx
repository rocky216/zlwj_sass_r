import React from "react"
import { connect } from "react-redux";


class TimeTaskPage extends React.Component {
  render() {
    return (
      <div>
        TimeTaskPage
      </div>
    );
  }
}

export default connect()(TimeTaskPage)