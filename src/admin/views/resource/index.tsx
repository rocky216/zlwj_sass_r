import React from "react"
import { connect } from "react-redux";


class ResourcePage extends React.Component {
  render() {
    return (
      <div>
        ResourcePage
      </div>
    );
  }
}

export default connect()(ResourcePage)